package nc.dssl.interpret.element.bracket;

import nc.dssl.interpret.Interpreter;

public abstract class RBracketElement extends BracketElement {
	
	protected RBracketElement(Interpreter interpreter) {
		super(interpreter);
	}
	
}
